(function () {
    'use strict';

    var app = angular.module('ShipperApp');

    app.controller('HomeCtrl', ['$scope', function ($scope) {
        var hc = this;

        var init = function(){
            
        };

        init();
    }]);

})();